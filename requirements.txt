# MediSimplifier v2 — Nemotron x VAGT

openai      # Nebius Token Factory client (OpenAI-compatible API)
numpy       # VAGT variance decomposition + bootstrap CIs
requests    # direct Token Factory HTTP calls (nemotron_judge_test.py)
tqdm        # progress bars
